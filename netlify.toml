  [build]
    command = "cd www & hugo --gc --minify"
    publish = "www/public"

  [build.environment]
    HUGO_VERSION = "0.120.4"
    HUGO_ENV = "production"

  [[headers]]
    for = "/*"
    [headers.values]
      X-Frame-Options = "DENY"
      X-XSS-Protection = "1; mode=block"
      X-Content-Type-Options = "nosniff"
